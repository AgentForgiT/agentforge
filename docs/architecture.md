# Architecture

The canonical architecture document lives at `.agentforge/architecture.md`.

This public docs entry mirrors the main idea: AgentForge starts as a modular monorepo with a `.agentforge/` project brain and extracts standalone repositories only when justified by accepted ADRs.
